-- ============================================================
-- 第十一轮改造增量：任务说明（模板默认 + 每天可改）
-- ============================================================

-- 模板上的默认说明（设一次，每天显示，可被当天覆盖）
alter table public.task_templates add column if not exists instruction text;

-- 当天说明（每天可改，空则用模板默认）
alter table public.daily_records add column if not exists instruction text;

-- ============================================================
-- 完成。模板设默认说明，每天能改当天的具体要求（如今天背第3课）。
-- ============================================================
