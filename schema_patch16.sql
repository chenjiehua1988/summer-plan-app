-- ============================================================
-- 第十六轮改造增量：连续打卡奖励防重复标记
-- ============================================================

-- 记录该孩子上次已奖励到第几个连续周期（每 streak_days 天一个周期）
-- 结算时 floor(streak/streak_days) > last_bonus_streak 才发奖，防止漏/重
alter table public.children add column if not exists last_bonus_streak int default 0;

-- ============================================================
-- 完成。配合"连续天数不算当天"的新规则：奖励按周期防重复发放。
-- ============================================================
